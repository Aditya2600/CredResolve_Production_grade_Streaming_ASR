fastapi==0.115.0
uvicorn[standard]==0.30.6
numpy==1.26.4
pandas
soundfile
torch==2.4.1
torchaudio==2.4.1
onnxruntime-gpu==1.20.1
huggingface_hub>=0.24.6
transformers==4.52.0
speechbrain==0.5.16
tqdm==4.66.5
prometheus-client==0.20.0
orjson==3.10.7
tritonclient[grpc,http]>=2.54,<2.67
websockets
pyrnnoise==0.4.3
soxr>=0.3.7
# Silero VAD — PyPI package ships bundled model weights so no git/network is
# needed at container startup. audio_processing.py prefers this over torch.hub.
silero-vad>=5.1.2
# Default denoiser; see docs/audio/denoiser-eval.md.
# RNNoise (pyrnnoise) is kept above as a one-release fallback (DENOISER=rnnoise).
# deepfilterlib (the Rust extension inside deepfilternet) requires Cargo at build
# time — the Dockerfile installs rustup before pip install, then removes it.
deepfilternet>=0.5.6
datasets<3.0.0
accelerate
librosa
nemo_toolkit[asr]==2.4.1
pyarrow==15.0.0
