global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "gateway"
    metrics_path: /metrics
    static_configs:
      - targets: ["gateway:8000"]

  - job_name: "worker"
    metrics_path: /metrics
    static_configs:
      - targets: ["worker:9000"]

  - job_name: "node"
    metrics_path: /metrics
    static_configs:
      - targets: ["node-exporter:9100"]

  # Optional GPU metrics (only if dcgm-exporter enabled)
  - job_name: "gpu"
    metrics_path: /metrics
    static_configs:
      - targets: ["dcgm-exporter:9400"]
