apiVersion: 1
providers:
  - name: "CredResolve"
    orgId: 1
    folder: "CredResolve ASR"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
